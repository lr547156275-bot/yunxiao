# Packaged configurations

This directory is populated by `package_v1_results.py` with immutable copies
of the semantic/formal manifests and the four case `config.txt` files. The
original files under `config/` and `cases/` are not modified.
