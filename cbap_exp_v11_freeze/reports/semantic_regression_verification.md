# Semantic regression verification

The existing report begins with `SEMANTIC_PASS`; 11/11 run directories have `completed.flag`. The processed pacing, admission, credit-scope, multibottleneck and increase-policy audits contain no hard failure.
